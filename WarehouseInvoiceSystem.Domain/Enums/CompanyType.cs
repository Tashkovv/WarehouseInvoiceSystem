namespace WarehouseInvoiceSystem.Domain.Enums
{
    public enum CompanyType
    {
        Client = 1,      // Company that pays us
        Vendor = 2,      // Company we pay
        Both = 3         // Both client and vendor
    }
}
