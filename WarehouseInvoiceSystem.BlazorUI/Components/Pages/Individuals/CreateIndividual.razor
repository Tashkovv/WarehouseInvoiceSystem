@page "/individuals/create"
@using WarehouseInvoiceSystem.Application.Interfaces
@using WarehouseInvoiceSystem.Application.DTOs.Individual
@inject IIndividualService IndividualService
@inject ILocalizationService Localization
@inject NavigationManager Navigation
@inject ISnackbar Snackbar

<PageTitle>@Localization.GetString("CreateIndividual") - @Localization.GetString("InvoiceSystem")</PageTitle>

<MudText Typo="Typo.h4" Class="mb-6">@Localization.GetString("CreateIndividual")</MudText>

<MudCard Elevation="2" Class="mb-4">
    <MudCardContent>
        <MudForm @ref="_form" @bind-IsValid="@_isValid">
            <MudGrid>
                <MudItem xs="12" md="6">
                    <MudTextField @bind-Value="_model.FirstName"
                                  Label="@Localization.GetString("FirstName")"
                                  Required="true"
                                  Variant="Variant.Outlined"
                                  RequiredError="@Localization.GetString("FirstNameRequired")"
                                  MaxLength="100" />
                </MudItem>

                <MudItem xs="12" md="6">
                    <MudTextField @bind-Value="_model.LastName"
                                  Label="@Localization.GetString("LastName")"
                                  Required="true"
                                  Variant="Variant.Outlined"
                                  RequiredError="@Localization.GetString("LastNameRequired")"
                                  MaxLength="100" />
                </MudItem>

                <MudItem xs="12" md="6">
                    <MudTextField @bind-Value="_model.IdentificationNumber"
                                  Label="@Localization.GetString("IdentificationNumber")"
                                  Required="true"
                                  Variant="Variant.Outlined"
                                  RequiredError="@Localization.GetString("IdentificationNumberRequired")"
                                  MaxLength="50" />
                </MudItem>

                <MudItem xs="12" md="6">
                    <MudTextField @bind-Value="_model.Phone"
                                  Label="@Localization.GetString("Phone")"
                                  Variant="Variant.Outlined"
                                  MaxLength="50" />
                </MudItem>

                <MudItem xs="12" md="6">
                    <MudTextField @bind-Value="_model.Email"
                                  Label="@Localization.GetString("Email")"
                                  Variant="Variant.Outlined"
                                  InputType="InputType.Email"
                                  MaxLength="100" />
                </MudItem>

                <MudItem xs="12" md="6">
                    <MudTextField @bind-Value="_model.Address"
                                  Label="@Localization.GetString("Address")"
                                  Variant="Variant.Outlined"
                                  MaxLength="500" />
                </MudItem>

                <MudItem xs="12">
                    <MudSwitch @bind-Value="_model.IsActive"
                               Label="@Localization.GetString("ActiveIndividual")"
                               Color="Color.Success" />
                </MudItem>
            </MudGrid>
        </MudForm>
    </MudCardContent>
</MudCard>
<!-- Actions -->
<MudItem xs="12">
    <div class="d-flex justify-end gap-2">
        <MudButton Variant="Variant.Outlined"
                   OnClick="Cancel"
                   Disabled="@_saving">
            @Localization.GetString("Cancel")
        </MudButton>
        <MudButton Variant="Variant.Filled"
                   Color="Color.Primary"
                   OnClick="SaveIndividual"
                   Disabled="@(!_isValid || _saving)">
            @if (_saving)
            {
                <MudProgressCircular Class="ms-n1" Size="Size.Small" Indeterminate="true" />
                <MudText Class="ms-2">@Localization.GetString("Saving")</MudText>
            }
            else
            {
                @Localization.GetString("Save")
            }
        </MudButton>
    </div>
</MudItem>

@code {
    private MudForm _form = null!;
    private bool _isValid;
    private bool _saving;
    private CreateIndividualDto _model = new() { IsActive = true };

    protected override async Task OnInitializedAsync()
    {
        Localization.OnLanguageChanged += OnLanguageChanged;
    }

    private async void OnLanguageChanged()
    {
        await InvokeAsync(StateHasChanged);
    }

    private async Task SaveIndividual()
    {
        await _form.Validate();
        if (!_isValid) return;

        _saving = true;

        try
        {
            await IndividualService.CreateIndividualAsync(_model);
            Snackbar.Add(Localization.GetString("IndividualCreated"), Severity.Success);
            Navigation.NavigateTo("/individuals");
        }
        catch (InvalidOperationException ex)
        {
            Snackbar.Add(ex.Message, Severity.Error);
        }
        catch (Exception ex)
        {
            Console.WriteLine($"Error creating individual: {ex.Message}");
            Snackbar.Add(Localization.GetString("ErrorCreatingIndividual"), Severity.Error);
        }
        finally
        {
            _saving = false;
        }
    }

    private void Cancel()
    {
        Navigation.NavigateTo("/individuals");
    }
}